const FooterLinkHeader = ({ title = "title" }: { title: string }) => {
  return <h4 className="mb-4.5 font-plex text-[14px] font-semibold uppercase tracking-widest text-gray-04">{title}</h4>;
};

export default FooterLinkHeader;
